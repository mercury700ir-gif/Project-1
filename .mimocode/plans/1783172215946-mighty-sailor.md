# Plan: RTL, Persian Numbers & Typography Fixes

## Current State Summary

- **3 HTML files**: `index.html`, `about.html`, `contact.html` — all already have `lang="fa" dir="rtl"`
- **1 CSS file**: `styles.css` (795 lines) — body has `direction: rtl`, fonts are IRANSans with 4 weights
- **1 JS file**: `script.js` (87 lines) — menu toggle, scroll animations, contact form handler
- **Fonts**: 4 IRANSans woff2 variants in `assets/fonts/`, properly declared via `@font-face`
- **Colors**: Custom properties defined (`--bg: #f7f3ec`, `--ink: #171a1f`, etc.) — keep as-is

## What Needs to Change

### 1. `styles.css` — Typography Alignment

Add explicit text-align rules that the user requested:

- `h1, h2, h3` → `text-align: right`
- `p` → `text-align: justify`
- `.lead` (already a `p`) inherits justify, no extra rule needed
- `.section-heading p` already styled, inherits from `p`

No other CSS changes needed — RTL direction, fonts, colors, mobile menu, cards, and buttons all render correctly in RTL already.

### 2. `script.js` — Add `toPersianNumbers()` Helper

Add a global helper function at the top of `script.js`:

```js
function toPersianNumbers(str) {
  const persianDigits = ["۰", "۱", "۲", "۳", "۴", "۵", "۶", "۷", "۸", "۹"];
  return String(str).replace(/[0-9]/g, (d) => persianDigits[+d]);
}
```

Then apply it to DOM text nodes on load so any Western digits in visible text become Persian. The function should NOT convert numbers inside:
- `href` attributes (tel:, mailto:)
- `dir="ltr"` inputs (phone, email fields)
- SVG attributes
- Email addresses

Implementation: a `DOMContentLoaded` listener that walks text nodes and replaces Western digits, skipping elements inside `[dir="ltr"]`, `<input>`, `<textarea>`, `<select>`, `<svg>`, and elements with `href` containing `tel:` or `mailto:`.

### 3. HTML Files — No Structural Changes Needed

All three HTML files already:
- Have `lang="fa" dir="rtl"` on `<html>`
- Use IRANSans font
- Have numbers already in Persian Unicode (۱۰, ۱, ۲, ۳)
- Have correct RTL layout for nav, cards, mobile menu

The phone number `+98 912 214 7417` in `contact.html:109` is in an LTR-styled `<strong>` — the `toPersianNumbers` function will skip it since the element has LTR direction.

## Files to Modify

| File | Change |
|------|--------|
| `styles.css` | Add `text-align: right` to h1/h2/h3, `text-align: justify` to p |
| `script.js` | Add `toPersianNumbers()` helper + DOMContentLoaded walker |
| `index.html` | No changes |
| `about.html` | No changes |
| `contact.html` | No changes |

## Verification

1. Open each HTML file in a browser
2. Confirm headings align right, paragraphs are justified
3. Confirm all visible numbers are Persian digits (except phone/email in LTR fields)
4. Confirm mobile menu opens/closes correctly in RTL
5. Confirm font is IRANSans throughout
6. Confirm body background/color scheme unchanged
